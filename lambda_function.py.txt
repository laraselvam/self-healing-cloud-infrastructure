import boto3
def lambda_handler(event,context):
    ec2=boto3.client('ec2',region_name='ap-southeast-2')
    instance_id='i-04421684b1adda45c'
    ec2.reboot_instances(instanceIds=[instance_id])
    return{
        'statuscode':200,
        'body':'instance rebooted successfully'
    }